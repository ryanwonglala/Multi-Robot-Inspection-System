# Cmd 20260531 Semantic Nav Check Runtime Fix

## Rebuild

```bash
cd /home/ryan/tb4_ws
colcon build --packages-select semantic_nav_check
source install/setup.bash
```

## Relaunch

```bash
ros2 launch semantic_nav_check verify.launch.py start_rviz:=true publish_initial_pose:=true
```

## Runtime Checks If Robot Does Not Move

```bash
ros2 action list | grep navigate
ros2 node list | grep -E 'bt_navigator|controller|planner|amcl|map_server'
ros2 topic echo /cmd_vel --once
ros2 run tf2_ros tf2_echo map base_link
ros2 lifecycle nodes
```
