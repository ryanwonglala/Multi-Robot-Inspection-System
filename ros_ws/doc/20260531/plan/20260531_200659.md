# Plan 20260531 Dry-Run Experiment Split

## Goal

Move the dry-run task layer into a separate experiment area so the formal `src/sim` ROS package stays focused on Gazebo/TurtleBot4 simulation glue.

## Structure

```text
experiments/task_layer_dryrun
```

This folder is for learning, testing task decomposition, and changing the task-layer design without affecting the ROS simulation package.

## Next Step

Use the experiment folder to explain and evolve the task logic first. After the design is clear, migrate stable pieces into a real ROS2 task-layer node or package.
