# Plan 20260531 Task Layer Dry Run

## Goal

Start the task-driven inspection layer with a small, readable dry-run loop.

## Scope

- Add structured task input YAML.
- Add robot capability YAML.
- Add named map location YAML.
- Add a task runner that prints the generated skill sequence.
- Do not command Nav2 or Gazebo yet.
- Do not modify `/opt/ros/jazzy`.

## Learning Focus

This step is meant to make the upper-level logic easy to understand before adding ROS2 action execution:

```text
task yaml -> plan skills -> check robot capabilities -> print execution plan
```

## Next Step

Explain `task_runner.py` line by line, then add the first real execution hook for `NavigateTo` using Nav2 only after the dry-run logic is clear.
