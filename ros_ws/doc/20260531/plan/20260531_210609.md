# Plan 20260531 Map-Only Semantic Correction

## Goal

Correct the semantic world model so it is based only on the current `src/sim/worlds/map.sdf`, not the old `demo.sdf` design file.

## Changes Planned

- Sync `src/sim/worlds/demo.sdf` to exactly match `src/sim/worlds/map.sdf`.
- Remove the old narrow-passage task target from dry-run examples.
- Keep the former passage only as a blocked zone in `world_model.yaml`.
- Re-run dry-run tasks for current valid map areas.

## Reason

The old `demo.sdf` still contained outdated narrow-passage geometry. The current map has that former narrow/service passage sealed after manual mapping attempts failed.
