# Debug 20260531 Sandbox Helper Failure

## Symptom

Normal sandboxed commands and the `apply_patch` tool failed with:

```text
bwrap: loopback: Failed RTM_NEWADDR: Operation not permitted
```

## Workaround Used

Commands were run with approval outside the sandbox. File edits were applied with a short Python writer scoped to `/home/ryan/tb4_ws` because the normal patch helper could not start.

## Impact

No files outside `/home/ryan/tb4_ws` were modified. `/opt/ros/jazzy` was not changed.
