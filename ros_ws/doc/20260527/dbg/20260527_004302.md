# Dbg 20260527_004302

## Reference

Old project GUI reference:

- `/home/ryan/grad_project_ws/src/mother_child_gui/mother_child_gui/child_robot_gui_controller.py`

The old GUI used PyQt5 and published to `/child_robot/cmd_vel`.

## New Choice

Use `tkinter` for the new TB4 GUI to avoid adding PyQt5 as a project dependency.

## Behavior

- Buttons: `W`, `A`, `S`, `D`, `STOP`
- Keyboard: `W`, `A`, `S`, `D`, `Space`
- Sliders: linear and angular speed
- Status: last command and odom display

## Safety

- Button release sends stop.
- Key release sends stop.
- Window close sends stop.

## Verification

```bash
python3 -m py_compile src/sim/scripts/ctl.py
source /opt/ros/jazzy/setup.bash
colcon build
source install/setup.bash
ros2 launch sim ctl.launch.py --show-args
```

Passed.

The GUI itself was not opened by Codex because it requires an interactive desktop session.
