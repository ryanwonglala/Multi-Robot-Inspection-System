# Plan 20260527_012232

## Next Manual Check

1. Launch Gazebo only:

```bash
ros2 launch sim map.launch.py slam:=false rviz:=false
```

2. In Gazebo, confirm wall models are individually visible/selectable.
3. Try selecting `wall_restricted_west` or `wall_service_east`.
4. If selection works, manually prototype the right-side redesign in GUI.
5. Save or report the desired final wall positions.

## If Selection Still Fails

Next approach:

- Create explicit model files under `models/wall_*` only if Gazebo GUI requires model database style entities.
- Or build a small SDF editing script that moves named `wall_*` models by command line.
