# Plan 20260527_012038

## Done

- Reverted incorrect east cutoff patch.
- Confirmed SDF XML parse passes.
- Confirmed `colcon build` passes.

## Proposed Next Step

Convert wall representation from:

```text
one model containing many wall links
```

to:

```text
many static wall models, one wall segment per model
```

## Why

This should make walls individually selectable and movable in Gazebo GUI.

## Suggested Workflow

1. Create a backup world file or script-based reversible migration.
2. Convert only `small_indoor_technical_facility_walls` first.
3. Keep floor and semantic markers unchanged.
4. Validate SDF XML parse.
5. Build.
6. Launch Gazebo and confirm all wall locations are unchanged.
7. Try selecting one wall segment in Gazebo GUI.
