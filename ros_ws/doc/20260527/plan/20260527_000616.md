# Plan 20260527_000616

## Next Manual Tests

1. Run Gazebo plus robot only:

```bash
ros2 launch sim map.launch.py slam:=false rviz:=false
```

2. If stable, run SLAM without RViz:

```bash
ros2 launch sim map.launch.py slam:=true rviz:=false
```

3. Check `/scan` and `map -> base_link`.

4. If stable, run the full UI:

```bash
ros2 launch sim map.launch.py
```

## If Gazebo Still Exits

Next debug target:

- Launch official `warehouse` with the same local launch and compare logs.
- Temporarily switch model to `lite`.
- Reduce custom world physics from `max_step_size 0.001` to the official `0.003`.
- Capture the latest `~/.ros/log/.../launch.log` and `gz sim server_*.log`.
