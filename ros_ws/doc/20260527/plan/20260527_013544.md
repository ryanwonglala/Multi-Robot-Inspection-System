# Plan 20260527_013544

## Next

1. Relaunch with ROS:

```bash
ros2 launch sim map.launch.py slam:=false rviz:=false
```

2. Confirm the manually edited map appears.
3. If still old, close all Gazebo processes and launch again from a freshly sourced terminal.
4. If correct, proceed with manual map cleanup and rebuild after each save.

## Rule

After editing `src/sim/worlds/map.sdf` manually in Gazebo GUI:

```bash
colcon build
source install/setup.bash
```

Then use `ros2 launch`.
