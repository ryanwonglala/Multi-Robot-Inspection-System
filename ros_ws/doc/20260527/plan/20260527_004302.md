# Plan 20260527_004302

## Manual Test

1. Start mapping launch:

```bash
ros2 launch sim map.launch.py
```

2. Start GUI:

```bash
ros2 launch sim ctl.launch.py
```

3. Test small motions with low speed.
4. Confirm release/STOP stops the robot.
5. Use GUI for manual SLAM mapping if stable.

## If GUI Does Not Move Robot

Check topic wiring:

```bash
ros2 topic echo /cmd_vel --once
ros2 topic list | rg 'cmd_vel|odom'
```

Then relaunch with explicit topics:

```bash
ros2 launch sim ctl.launch.py cmd:=/cmd_vel odom:=/odom
```
