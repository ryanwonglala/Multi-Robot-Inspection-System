# Plan 20260527_015920

## Current State

The project has a working TurtleBot4 simulation, manual control path, SLAM workflow, editable wall-model world structure, and a saved baseline map.

## Next Session

1. Visually inspect `maps/base_20260527.pgm`.
2. Decide whether the baseline map is good enough for navigation experiments.
3. If needed, use Gazebo GUI to adjust `wall_*` models in `src/sim/worlds/map.sdf`.
4. Rebuild after manual world edits.
5. Re-map and save a second baseline if geometry changes are significant.
6. Start planning Nav2 localization on the saved map.

## Open Questions

- Whether the upper-right region still creates SLAM drift after manual geometry edits.
- Whether the saved map should be promoted from `base_20260527` to the canonical `base` map.
- Whether to create a smaller project-specific RViz config for mapping only.
