# Cmd 20260527_013544

## Sync Source World To Launch World

```bash
cd /home/ryan/tb4_ws
source /opt/ros/jazzy/setup.bash
colcon build
source install/setup.bash
```

## Confirm Source And Install Match

```bash
python3 - <<'PY'
from pathlib import Path
import hashlib
for f in ['src/sim/worlds/map.sdf', 'install/sim/share/sim/worlds/map.sdf']:
    print(f, Path(f).stat().st_size, hashlib.sha256(Path(f).read_bytes()).hexdigest())
PY
```

Expected current hash:

```text
cc746e097d1dbb71fbc0174087f1d679ccb6eea659bad09fcb539074b6132e36
```

## Confirm Launch Package Share

```bash
source /opt/ros/jazzy/setup.bash
source install/setup.bash
python3 - <<'PY'
from ament_index_python.packages import get_package_share_directory
print(get_package_share_directory('sim'))
PY
```

Expected:

```text
/home/ryan/tb4_ws/install/sim/share/sim
```
