# Cmd 20260527_000616

## Rebuild

```bash
cd /home/ryan/tb4_ws
source /opt/ros/jazzy/setup.bash
colcon build
source install/setup.bash
```

## Test A: Gazebo And Robot Only

Use this first to isolate Gazebo stability from SLAM/RViz:

```bash
ros2 launch sim map.launch.py slam:=false rviz:=false
```

Expected:

- Gazebo stays open.
- `map.sdf` loads.
- TurtleBot4 appears near the entrance.
- No quick Gazebo exit.

## Test B: Add SLAM Without RViz

```bash
ros2 launch sim map.launch.py slam:=true rviz:=false
```

In another terminal:

```bash
source /opt/ros/jazzy/setup.bash
source /home/ryan/tb4_ws/install/setup.bash
ros2 topic echo /scan --once
ros2 run tf2_ros tf2_echo map base_link
```

Expected:

- `/scan` publishes.
- `map -> odom -> base_link` becomes available after SLAM activates.

## Test C: Full Mapping UI

```bash
ros2 launch sim map.launch.py
```

Teleop terminal:

```bash
source /opt/ros/jazzy/setup.bash
source /home/ryan/tb4_ws/install/setup.bash
ros2 run teleop_twist_keyboard teleop_twist_keyboard
```

Save map after exploration:

```bash
ros2 run nav2_map_server map_saver_cli -f maps/base
```
