# Cmd 20260527_015920

## Save Map Command Used

```bash
cd /home/ryan/tb4_ws
source /opt/ros/jazzy/setup.bash
source install/setup.bash
ros2 run nav2_map_server map_saver_cli -f maps/base_20260527
```

## Check Saved Map

```bash
find maps -maxdepth 1 -type f -printf '%TY-%Tm-%Td %TH:%TM:%TS %s %p\n' | sort
cat maps/base_20260527.yaml
file maps/base_20260527.pgm
```

## After Manual World Editing

When `src/sim/worlds/map.sdf` is saved from Gazebo GUI:

```bash
source /opt/ros/jazzy/setup.bash
colcon build
source install/setup.bash
```

Then relaunch:

```bash
ros2 launch sim map.launch.py
```
