# Cmd 20260527_012232

## Validate SDF Structure

```bash
cd /home/ryan/tb4_ws
python3 - <<'PY'
import xml.etree.ElementTree as ET
root = ET.parse('src/sim/worlds/map.sdf').getroot()
world = root.find('world')
models = [m.attrib.get('name') for m in world.findall('model')]
wall_models = [m for m in models if m and m.startswith('wall_')]
print('xml ok')
print('wall models', len(wall_models))
print('old grouped model present', 'small_indoor_technical_facility_walls' in models)
PY
```

Expected:

```text
xml ok
wall models 43
old grouped model present False
```

## Build

```bash
source /opt/ros/jazzy/setup.bash
colcon build
source install/setup.bash
```

## Launch

```bash
ros2 launch sim map.launch.py slam:=false rviz:=false
```
