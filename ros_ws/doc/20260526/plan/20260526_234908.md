# Plan 20260526_234908

## Goal

Complete a repeatable TurtleBot4 mapping path in `tb4_ws`.

## Now

- Official TurtleBot4 simulation works.
- TurtleBot4 can move.
- Custom world files exist in `src/sim/worlds`.

## Next

1. Add install rules for `src/sim/launch` and `src/sim/worlds`.
2. Add short local launch file for custom world + TurtleBot4 spawn.
3. Enable official TurtleBot4 SLAM from launch.
4. Build and source the workspace.
5. Launch simulation and manually map the facility.
6. Save baseline map to `maps/base.yaml` and `maps/base.pgm`.

## Rule For Future Work

When AI changes code, also update one of:

- `doc/log`
- `doc/cmd`
- `doc/dbg`
- `doc/plan`

The project should remain explainable from files alone.
