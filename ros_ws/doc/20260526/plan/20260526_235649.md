# Plan 20260526_235649

## Done

1. Added install rules for `launch` and `worlds`.
2. Added short launch file: `src/sim/launch/map.launch.py`.
3. Wired custom world loading to official TurtleBot4 spawn.
4. Enabled official TurtleBot4 SLAM and RViz by default.
5. Built the workspace successfully.
6. Verified launch argument parsing.

## Manual Next

1. Run `ros2 launch sim map.launch.py`.
2. Confirm Gazebo loads `map.sdf`.
3. Confirm TurtleBot4 spawns near the entrance.
4. Teleoperate through the facility.
5. Save `maps/base.yaml` and `maps/base.pgm`.

## If Spawn Pose Is Bad

Relaunch with short overrides:

```bash
ros2 launch sim map.launch.py x:=0.0 y:=-5.0 yaw:=1.5708
```
